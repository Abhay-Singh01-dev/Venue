[project]
name = "flowstate-ai-backend"
version = "2.4.1"
description = "FastAPI backend for FlowState AI crowd intelligence system"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true